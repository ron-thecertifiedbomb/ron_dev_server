# 🧩 Personal Node.js Server — Testing & CRUD Playground

This project serves as a **personal backend playground** for experimenting with Node.js, Express, and MongoDB.  
You can use it to test **CRUD operations**, **array methods**, **file uploads**, and other custom **utility functions** or **API experiments** before integrating them into real apps.

---

## 🚀 Features

- ✅ Basic CRUD operations using Express + MongoDB  
- ⚙️ Modular folder structure (routes, controllers, services, models)  
- 🧠 Playground for JavaScript logic (array methods, functions, etc.)  
- 📁 File upload support (using `multer` or similar library)  
- 🧰 Perfect for backend testing & API prototyping  

---

## 📂 Folder Structure

server/
├── config/ # Database config, environment setup
│ └── db.js
├── controllers/ # Handles request/response logic
├── models/ # Mongoose schemas
├── routes/ # Express routes
├── services/ # Business logic layer
├── utils/ # Helper functions, array methods, etc.
├── uploads/ # File uploads directory
└── server.js # App entry point

## 🛠️ Installation & Setup

1. **Clone the repository**
   ```bash
   git clone https://github.com/your-username/CRUD_Application_Node.git
   cd CRUD_Application_Node